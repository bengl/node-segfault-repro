{
  "scripts": {
    "test": "jest --detectOpenHandles",
    "docker:build": "docker build -t demo-segfault .",
    "docker:run": "docker run -t demo-segfault"
  },
  "dependencies": {
    "jest": "27.0.4"
  },
  "license": "MIT"
}
